apiVersion: apps/v1
kind: Deployment
metadata:
  name: goshop-review-srv
  labels:
    app.kubernetes.io/part-of: goshop
    app.kubernetes.io/name: goshop-review-srv
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/part-of: goshop
      app.kubernetes.io/name: goshop-review-srv
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: goshop
        app.kubernetes.io/name: goshop-review-srv
    spec:
      containers:
        - name: review
          image: goshop/review:latest
          ports:
            - {name: grpc, containerPort: 9005}
            - {name: metrics, containerPort: 9055}
          envFrom:
            - secretRef: {name: goshop-review-secrets}
          readinessProbe:
            httpGet: {path: /readyz, port: metrics}
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /livez, port: metrics}
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: goshop-review-srv
  labels:
    app.kubernetes.io/part-of: goshop
    app.kubernetes.io/name: goshop-review-srv
spec:
  selector:
    app.kubernetes.io/part-of: goshop
    app.kubernetes.io/name: goshop-review-srv
  ports:
    - {name: grpc, port: 9005, targetPort: grpc}
    - {name: metrics, port: 9055, targetPort: metrics}
